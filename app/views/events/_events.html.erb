<div class="event">
  <h2>Name: <%= link_to event.name, event_path(event)%> </h2>
  <h3>City: <%= event.city %> </h3>
  <h4>Date: <%= event.date %> </h4>
  <p>Capacity: <%= event.capacity %> </p>
</div>

<%= link_to "Event", event_path(event) %>
